function palindromeNumberGenerator ( number ) {
    var isPalindrome = reversedNum;
    var stepCounter = 0;

    function checkPalindrome ( number ) {
      var reversedNum = number.toString().split('').reverse().join('');
      console.log(checkPalindrome(7658));
    }

    while (number !== reversedNum) {
      var newNum = add(number + reversedNum);
      newNum = number;
      stepCounter++;
    }
}